
<% content_for :show_d3 do %>

    <!-- Custom, External JS libraries -->
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true,  'data-turbolinks-suppress-warning'=> true  %>
    
    <!-- Bootstrap grid setup --> 
    <nav class="navbar navbar-default">
        <div class="container">
            <a class="navbar-brand" href="#">
                <%= image_tag "logo.png",  height:"51", width:"72" %>
            </a>
            <div id="chart"></div>
        </div>
    </nav>


    <div class="container">
        <%= content_tag "div", id: "star", data: {revenues: Revenue.all} do %>
        <% end %>
        <%= javascript_tag do %>
            var myRe = $('#star').data('revenues');
            dispRevenue(myRe);
        <% end %>
    </div>
    
<% end %>